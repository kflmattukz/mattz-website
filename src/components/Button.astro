---
import { type HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<"button"> {
  className?: string;
}
const {className, ...props} = Astro.props;
---
<button class={className} {...props}><slot/></button>
