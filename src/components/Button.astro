---
import type { HTMLAttributes } from 'astro/types'
interface Props extends HTMLAttributes<"button"> {

}

const {...rest} = Astro.props;
---

<button {...rest} class="bg-textblack text-bgbase font-bold rounded-xl px-5 py-3 hover:scale-110 hover:transition duration-300 active:scale-90">
  <slot/>
</button>
